#include<stdio.h>
#include<math.h>
void main()
{
int n,p,r;
printf("Enter the number");
scanf("%d",&n);
printf("Enter the power");
scanf("%d",&p);
printf("The power of the given instance is %f",pow(n,p));
}
